{%- macro netdata_block(config) %}
  {% for section, settings in config.items() %}
[{{ section }}]
      {%- for key, value in settings.items() %}
    {{ key }} = {{ value }}
      {%- endfor -%}
  {% endfor %}
{%- endmacro -%}

# netdata configuration
#
# **** DO NOT EDIT THIS FILE ****
#
# This file is managed by Salt via {{ source }}
{{ netdata_block(config) }}
 
